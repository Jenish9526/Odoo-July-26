# Services Directory

Configure HTTP client instances here:
- `api.js`: Axios instance mapping base URLs, standard headers, and request/response interceptors to refresh JWTs automatically.
- API wrapper services (e.g. `authService.js`, `analyticsService.js`).
